# Build output (CMake) — regenerated by `cmake .. && make`, never commit
build/

# Compiled object files / deps
*.o
*.d
*.obj

# Editor / IDE
*.swp
.vscode/

# Local Claude settings (keep shared config, ignore machine-local)
.claude/settings.local.json
